import 'package:flutter/material.dart';

import 'app/app.dart';

/// Entry point of the magazine application.
///
/// The [main] function instantiates the root widget.  Flutter
/// applications always start by calling runApp() with a widget
/// that becomes the root of the widget tree.
void main() {
  runApp(const MyApp());
}